(()=>{
const app=document.getElementById('app'),home=document.getElementById('home'),toastEl=document.getElementById('toast');if(!app||!home)return;
const NAME_KEY='calculator-account-name',HASH_KEY='calculator-account-password-hash';
if(!localStorage.getItem(NAME_KEY))localStorage.setItem(NAME_KEY,'Clayton Whitson');
const toast=m=>{if(!toastEl)return;toastEl.textContent=m;toastEl.classList.add('show');setTimeout(()=>toastEl.classList.remove('show'),1300)};
const show=el=>{document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));el.classList.add('active')};
async function hash(s){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode('calculator-account-v36:'+s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function shell(title){app.innerHTML='';const p=document.createElement('div');p.className='page settings-page';const h=document.createElement('div');h.className='head settings-head';const b=document.createElement('button');b.className='back';b.textContent='‹ Settings';b.onclick=()=>window.CalculatorSettings?.open?.()||show(home);const t=document.createElement('h2');t.textContent=title;h.append(b,t);p.append(h);app.append(p);show(app);return p}
function open(){const p=shell('Calculator Account');const profile=document.createElement('div');profile.className='settings-profile';profile.innerHTML='<div class="settings-profile-icon">CW</div><div><strong></strong><span>Signed in to this Calculator</span></div>';profile.querySelector('strong').textContent=localStorage.getItem(NAME_KEY)||'Clayton Whitson';p.append(profile);const card=document.createElement('div');card.className='settings-form-card';const name=document.createElement('input');name.className='field';name.value=localStorage.getItem(NAME_KEY)||'Clayton Whitson';name.placeholder='Account name';name.autocomplete='off';const pw=document.createElement('input');pw.className='field';pw.type='password';pw.placeholder=localStorage.getItem(HASH_KEY)?'New Calculator Account password':'Set Calculator Account password';pw.autocomplete='new-password';const confirm=document.createElement('input');confirm.className='field';confirm.type='password';confirm.placeholder='Confirm password';confirm.autocomplete='new-password';const save=document.createElement('button');save.className='primary settings-save';save.textContent='Save Account';save.onclick=async()=>{const n=name.value.trim();if(!n)return toast('Enter a name');localStorage.setItem(NAME_KEY,n);if(pw.value){if(pw.value.length<6)return toast('Use at least 6 characters');if(pw.value!==confirm.value)return toast('Passwords do not match');localStorage.setItem(HASH_KEY,await hash(pw.value))}toast('Calculator Account saved');setTimeout(()=>window.CalculatorSettings?.open?.(),450)};card.append(name,pw,confirm,save);p.append(card);const note=document.createElement('div');note.className='settings-note';note.innerHTML='<strong>Important:</strong> This is a Calculator Account password, not your Apple Account password. Never enter your real Apple password here.';p.append(note)}
window.CalculatorAccount={open,name:()=>localStorage.getItem(NAME_KEY)||'Clayton Whitson',hasPassword:()=>!!localStorage.getItem(HASH_KEY)};
})();